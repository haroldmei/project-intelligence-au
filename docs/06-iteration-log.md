# Iteration Log

> One-line per iteration. Format: `- [YYYY-MM-DD] iter-N: <verdict> — <one-line summary>`

- [2026-07-03] iter-6: SHIPPED-LOCAL (issue #13) — PCC (Construction Certificate) feed as the "work starting now" timing signal (docs/24 G11). DA/CDC data says work *might* happen in 6–18mo; a CC means it's about to START. New Online PCC Data API adapter `src/modules/ingestion/pcc.ts` mirrors the Online DA API adapter (`fetchNswPlanningDAs`): `x-api-key` (reuses `NSW_PLANNING_API_KEY`), council + `since` incremental filter, **pagination** (page loop, 200/page, 10-page cap, boundary dedupe), and `mapPccCertificate`/`isConstructionCertificate` that keep **Construction Certificates only** (OC/SC dropped, long-label + "CC"-code robust). Linkage runner `pcc-ingest.ts`: `linkCertificate` upserts `constructionCertifiedAt` onto the DA via `updateMany` on the (`da_id`,`council`) unique — `count:0` = `unmatched` (never inserts a bare CC as a DA, never throws P2025); `runPccIngest` iterates the 15 LGAs, no-ops (`skipped`) unless `PCC_INGEST_ENABLED` **and** `NSW_PLANNING_API_KEY` are both set, wired into the nightly ingest cron AFTER `runIngest` (DAs exist first), one council failing isolated to Sentry. Storage: single nullable `construction_certified_at DATE` column on `development_applications` (hand-written migration `20260703050000`), mirroring `determination_date` — simpler than a `PostConsentCertificate` table for the v1 date+badge signal. Deliberately NOT logged to `ingestion_log` (its per-council drift check averages daCount across sourceApis — a CC-link count would corrupt DA drift). Surfaced: new green "CC issued — work starting" badge (`ConstructionCertBadge`, tz-safe yyyy-mm-dd formatter) on the portal DA cards (threaded through `CandidateDA`→loaders→`DACard`) and the email digest (inlined `success` hue); the rerank prompt receives it as a "work starting now — strongest timing signal" line (added to `CandidateDA`/`RerankCandidate`, `filters.ts` SQL SELECT, pipeline mapping, `renderUserPrompt`). Tests: pure CC-type filter + mapping (CC vs OC vs missing-ref/number), paginated fetch (CC-only filter, full+partial page, dedupe) with the network mocked, DA linkage found + not-found + wrong-council-no-match against the real test Postgres, `runPccIngest` flag-off no-op, and the badge rendering in the digest card + email + dedicated badge component tests. typecheck/lint green; fe 162 + backend 253 pass. Market re-analysis skipped — scoped feature off an existing roadmap item (G11), no market delta.

- [2026-07-03] iter-3: SHIPPED-LOCAL (issue #25) — waitlist demand instrument (expansion Wave 0, docs/25 §2). New `WaitlistEntry` model (email CITEXT, free-text trade + region, optional note, `source` landing|signup, `@@unique([email,trade,region])`) + hand-written migration; unauthenticated `POST /api/waitlist` — same 5/IP/min limiter as signup, honeypot (`company`) silently dropped, zod-validated (`src/modules/waitlist/schemas.ts`), idempotent via `upsert` on the composite unique with email/trade/region lowercased in code (so dedupe holds even where the test DB pushes email as plain text). Landing page gains a "Not a Sydney roofer?" `#waitlist` section (client `WaitlistForm`, confirmation "we'll email you when <trade> in <region> opens", **no** confirmation email — Spam Act surface avoided); onboarding `/onboarding/area` copy links out-of-scope users to it (no in-scope branch existed). Wedge copy untouched; no authenticated surface added. Tests: schema (validation/honeypot/normalise) + route integration (dedupe, case-insensitive dedupe, 422/400, honeypot-drop, rate-limit 429) + component (happy path + error). typecheck/lint green; fe 77 + backend 174 pass; migration applies to a fresh `migrate deploy` with `email` confirmed citext. Market re-analysis skipped — scoped instrument, no market delta.

- [2026-07-03] iter-5: SHIPPED-LOCAL (issue #14) — honest three-class lead framing (docs/24 §2.1: DA data structurally misses like-for-like re-roofs, exempt under Codes SEPP 2008 s2.53(c), so we stop implying exhaustive re-roof coverage). New **pure** classifier `src/modules/relevance/lead-class.ts`: deterministic rules over approval pathway + description/scope/type keywords → `builder_pipeline | fast_track | strata_heritage`, precedence strata&heritage → fast-track(CDC) → builder-pipeline (default = ambiguous fallback). Pathway input is **optional** — #10 (CDC ingest + `approvalPathway`) hasn't merged, so classification runs keyword-only today and upgrades automatically once the pathway is populated; SA "Code Assessed" is deliberately NOT treated as CDC. Persisted on `DigestDa.leadClass` (hand-written migration `20260703040000_add_lead_class`, `NOT NULL DEFAULT 'builder_pipeline'` so history is stable + backfills in place); classified once in `assemble.ts`, reused for the persisted row and the email card. Email digest **grouped** by class (fast-track → strata&heritage → builder-pipeline) with a subtle per-group header + per-card badge (SMS omitted — char budget); portal digest cards + history list get the same badges (`LeadClassBadge`, three subtle `Badge` variants; palette + WCAG in docs/03b §2b). Landing copy de-risked: sub-headline now "DA, CDC and strata signals across 15 Sydney LGAs — builder pipeline, fast-track CDC, and strata & heritage leads"; the "70% genuine re-roof jobs" and Cordell-triage lines reframed across the three classes; hero mockup shows one card per class. No marketing string claims exhaustive re-roof coverage. Tests: classifier fixtures per class + CDC-pathway + heritage-beats-CDC + SA-fallthrough + ambiguous/empty fallback + grouping/coercion helpers (backend, pure); email lead-class snapshot — all three badges, group order, within-group rank preserved, no-leadClass→builder default (fe suite; added `src/emails/**` to the fe include). typecheck/lint green; fe 154 + backend 240 pass. Market re-analysis skipped — scoped honesty/positioning fix off an existing roadmap item, no market delta.

- [2026-07-03] iter-4: SHIPPED-LOCAL (issue #20) — mid-week storm brief from BOM severe-weather warnings, flag-gated behind `STORM_BRIEF_ENABLED` (default off until dogfooded, docs/24 §4 August item 5). New `src/modules/weather/` module: `feed.ts` fetches the public BOM **NSW warnings RSS** (`www.bom.gov.au/fwo/IDZ00060.warnings_nsw.xml`, overridable via `BOM_WARNINGS_URL`) via the ingestion `fetchTextWithRetry`; `parse.ts` (pure, cheerio xmlMode) extracts warning id (IDN##### product code), type (severe thunderstorm / severe weather only — floods/marine dropped), issue time, affected-area text; `lgas.ts` is the static BOM-area→LGA keyword map ("Sydney Metropolitan"/"metropolitan"→all 15; towns→their LGA) + name lookup; `select.ts` is the pure send-selection/dedupe decision (one brief per affected user per warning). `cron.ts` orchestrates: no-op when the flag is off, else fetch → load opted-in subscribers (`emailVerified`+active/trial+`emailOptIn`+`stormBriefOptIn`) → dedupe against the new `StormBrief` table (`@@unique([warningId,userId])`, row committed BEFORE send so a warning live across 3-hourly ticks re-briefs nobody) → send. New `storm-brief` email template (BOM © attribution, one line on insurance-funded roofing demand) registered in the email client. `vercel.json` cron `/api/cron/storm-brief` every 3h with standard cron-secret auth. Per-user opt-out: `User.stormBriefOptIn` (default true), `setStormBriefOptIn` service + DTO field, `POST /api/account/storm-brief`, `/account/storm-brief` toggle page + link. Email-only in v1 (no SMS — Spam Act + cost). Tests: parse fixtures, LGA matching, select dedupe (fe suite, no DB/network) + backend cron integration (flag-off no-op, exactly-one-brief-per-user, no double-send on re-tick, opt-out + email-opt-out exclusion) + toggle component test + email snapshot. Also fixed a latent bug: the `(portal)` fast-glob test include silently matched nothing (unescaped route-group parens) — escaped so the new page test runs. typecheck/lint green; fe 101 + backend 193 + email snapshot 30 pass; migration applies clean. Market re-analysis skipped — scoped feature off an existing roadmap item, no market delta.
- [2026-04-29] iter-0: SHIPPED-LOCAL — preview-tier build complete; dogfood health 8.4 → POLISH round → 5/5 polish items shipped; commit `de593ff` tagged `v0.1.0-preview-ready`; deploy hand-off owned by user (DB + 12 env vars + `vercel link && vercel --prod`).
- [2026-07-02] iter-2: SHIPPED-LOCAL (issue #28) — formalised the jurisdiction adapter seam (platform seam #1, docs/25 §4) with zero NSW behaviour change. Formal `JurisdictionAdapter` interface (`id`, `capabilities{hasValue,pathwaysSupported}`, `fetchApplications({since,regions})`) in `jurisdictions/types.ts`; NSW ePlanning+DAEX sources wrapped as the single always-on `nsw` adapter (`jurisdictions/nsw.ts`); registry keyed by jurisdiction id (server-side `registry.ts` + pure jsdom-safe `config.ts`), `runIngest` iterates enabled jurisdictions × regions (today NSW's 15 councils, byte-identical order + per-council log/drift). Collapsed #29's provisional SA interface into the formal one. Prisma `jurisdiction` column (default `'nsw'`, backfilled) on `development_applications` + `lgas`. Timezone groundwork: per-jurisdiction `{timezone,currency,digest anchor}` config; DST-correct `weekStartInZone`/`digestWeekWindow` (Australia/Sydney) replacing the fixed-UTC+10 `weekStartAEST`; digest cron reads the "Sunday 18:00 local" window from the nsw config. Tests: DST-boundary week windows (autumn 169h / spring 167h fixtures), registry iteration, jurisdiction stamping on upsert. typecheck/lint green; fe 67 + backend 133 pass; migration applies to the docker test DB and a fresh `migrate deploy`. Market re-analysis skipped — scoped platform-seam refactor, no market delta.
- [2026-07-02] iter-1: SHIPPED-LOCAL (issue #23) — Spam Act unsubscribe SLA: sender-id + STOP footer centralised in `src/lib/sms/client.ts` (`applyComplianceWrapping`, enforced in `sendSms` — unbypassable); digest re-checks `smsOptIn`/`emailOptIn` from DB at send time so a mid-run STOP/unsubscribe suppresses that user's send; new token-based unauthenticated `GET /api/unsubscribe/[token]` (no login/fee) flips `User.emailOptIn`, gated in digest + trial-reminder crons; real-handler STOP webhook test + 18 new tests. typecheck/lint/unit green.
