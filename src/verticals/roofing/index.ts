// Roofing vertical pack — V1 baseline, always on (see src/verticals/registry.ts).
// The manifest and its parts live in ./pack, ./vocabulary, ./prompt-fragment.md
// and ./development-types; this barrel is the registry's import surface.
export { roofingPack } from "./pack";
